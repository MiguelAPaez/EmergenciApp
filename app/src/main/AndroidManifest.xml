<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.miguelapaez.emergenciapp">

    <uses-permission android:name="android.permission.CALL_PHONE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".FamilyGroupHealth"></activity>
        <activity android:name=".HealthQuestion" />
        <activity android:name=".FamilyGroup" />
        <activity android:name=".activity_registro_familiar" />
        <activity android:name=".activity_buscarFamiliar" />
        <activity android:name=".EmergencyOptions" />
        <activity android:name=".Profile" />
        <activity android:name=".HealthRegister" />
        <activity android:name=".Register" />
        <activity android:name=".Login" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>